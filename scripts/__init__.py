"""Operational CLI for REIG bootstrap + day-2 tasks.

Intended to be invoked via:
    docker compose run --rm api python -m scripts.cli <command>
so it runs inside the container with the same env + network as the API
process. See scripts/cli.py for the command list.
"""
